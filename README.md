# satyayuga
for vibe coding

## Love Simulator 开发说明

### 环境依赖检测
运行以下命令检查必备环境与依赖是否缺失：

```bash
npm run check:env
```

检测内容包括：
- Node.js 版本
- npm 是否可用
- 依赖是否安装完整（node_modules）

如有缺失依赖，请先执行：

```bash
npm install
```
